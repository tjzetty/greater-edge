{
  "name": "greater-edge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && firebase deploy",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.19.6",
    "firebase": "latest"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "vite": "^5.2.0"
  }
}
